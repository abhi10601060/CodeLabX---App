<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.codelabx">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.CodeLabX"
        android:allowBackup="true"
        android:icon="@mipmap/clx_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/clx_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CodeLabX"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.activities.AuthenticationActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="false"/>
    </application>

</manifest>